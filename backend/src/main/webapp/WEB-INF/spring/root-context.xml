<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xsi:schemaLocation="
        http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context.xsd">

    <!--
        컴포넌트 스캔 설정
        - 지정된 패키지에서 @Component, @Service, @Repository, @Controller 등을 자동으로 스캔
    -->
    <context:component-scan base-package="org.scoula.backend" />

    <!--
        프로퍼티(설정값) 파일 등을 로딩하려면 사용
        예: <context:property-placeholder location="classpath:db.properties"/>
    -->

    <!--
        필요한 Bean 등록은 여기에 추가적으로 작성하면 됨
    -->

</beans>